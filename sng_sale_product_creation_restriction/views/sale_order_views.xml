<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_disable_product_creation" model="ir.ui.view">
        <field name="name">sale.order.form.disable.product.creation</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Formulario de la línea de cotización. -->
            <xpath expr="//field[@name='order_line']/form//field[@name='product_id']" position="attributes">
                <attribute name="options">{
                    'no_create': True,
                    'no_create_edit': True,
                    'no_quick_create': True,
                }</attribute>
            </xpath>

            <!-- Lista editable: variante de producto (campo opcional). -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_id']" position="attributes">
                <attribute name="options">{
                    'no_open': True,
                    'no_create': True,
                    'no_create_edit': True,
                    'no_quick_create': True,
                }</attribute>
            </xpath>

            <!-- Lista editable: producto, campo mostrado por defecto. -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_template_id']" position="attributes">
                <attribute name="options">{
                    'no_open': True,
                    'no_create': True,
                    'no_create_edit': True,
                    'no_quick_create': True,
                }</attribute>
            </xpath>
        </field>
    </record>
</odoo>
