<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_consolidated_payment" model="ir.ui.view">
        <field name="name">account.move.form.consolidated.payment</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_open_consolidated_payment"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-sitemap"
                    invisible="not consolidated_payment_id">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Pago</span>
                        <span class="o_stat_text">Consolidado</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
